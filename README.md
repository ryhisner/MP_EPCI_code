The NDJSON files used in these functions were made using Nextclade CLI, with the following terminal commands:
               nextclade run -D ref_data -N filename.ndjson fastafile.fasta
               
